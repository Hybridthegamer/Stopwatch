/* This file is for extra functionality to be added. */
/* Highly unecessary but created just in case. */
#include "graphics.h"

int main(){
    while(1){
            displayWindow();
    }
    return 0;
}